# scanners-and-such

A library for working with barcode scanners and printers. All features should
support native platforms and web via WASM (where possible).

It currently supports SNAPI (including imaging interface) on some scanners.
